# PA 项目 Claude Code 入口

开始任何任务前，必须阅读并遵守：

1. `AGENTS.md`

`AGENTS.md` 是本项目统一的 AI 协作规则。Codex 还会继承全局 `~/.codex/AGENTS.md` 中的通用工作流。

项目知识位于 `docs/wiki/`，工程规则位于 `docs/rules/`，均按任务需要读取；每个 Issue 的过程文档集中放在 `docs/issues/<issue-id>-<slug>/`。

Claude Code 需遵守 `AGENTS.md` 中的项目规则；通用工作流由其运行环境单独配置。
