# PA 项目 Claude Code 入口

开始任何任务前，必须阅读并遵守：

1. `AGENTS.md`
2. `docs/ai-coding-workflow.md`

`AGENTS.md` 是本项目统一的 AI 协作规则，`docs/ai-coding-workflow.md` 是 Claude Code 与 Codex 共享的工作流。

项目知识位于 `docs/wiki/`，工程规则位于 `docs/rules/`，均按任务需要读取；每个 Issue 的过程文档集中放在 `docs/issues/<issue-id>-<slug>/`。

项目默认使用 Superpowers 工作流。未经用户批准，不得跳过需求澄清和技术方案直接编码。
